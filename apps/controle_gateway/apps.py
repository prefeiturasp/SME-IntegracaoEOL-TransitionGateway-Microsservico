from django.apps import AppConfig


class ControleGatewayConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.controle_gateway"
    verbose_name = "Controle Gateway"
